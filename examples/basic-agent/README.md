# Basic Agent Example

This example introduces the smallest useful agent workflow.

## Flow

1. Receive an input
2. Apply a clear set of instructions
3. Return a structured result

The implementation will be added when we begin building the shared foundation.
